# Every build tree, however it is configured. Listing them one at a time got
# as far as build/ and build-ub/ before a coverage tree turned up untracked.
build*/
cache/
out/
*.exe
*.o
*.obj
*.gcda
*.gcno
__pycache__/

